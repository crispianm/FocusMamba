# FocusMamba — Degradation-Robust Metric Video Depth Estimation
# =================================================================
# Install PyTorch (cu128+) first:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
# Then:
#   pip install -r requirements.txt

# ---- Core deep learning ----
# torch>=2.2
# torchvision>=0.17 # should be installed from above
einops>=0.7

# ---- Mamba / SSM (vendored in repo, but listed for reference) ----
# mamba-ssm        # pip install mamba-ssm (requires CUDA)
# causal-conv1d

# ---- Data / vision ----
numpy>=1.26
opencv-python>=4.8
imageio[ffmpeg]>=2.33
albumentations>=1.3
kornia>=0.7

# ---- Losses / metrics ----
pytorch-msssim>=1.0.0

# ---- Logging & experiment tracking ----
tensorboard>=2.15
matplotlib>=3.8
seaborn>=0.13

# ---- Configuration ----
pyyaml>=6.0

# ---- Evaluation / reporting ----
pandas>=2.1
tqdm>=4.66

# ---- Profiling ----
fvcore>=0.1.5.post20221221

# ---- Testing ----
pytest>=7.4
